var x = 5;

x = parseInt(x);

console.log(x);

if(x == 5){
    console.log("Here")
}
//javascript will try to convert types so it can run. does not want to crash
// === means to compare and dont change types
//NaN means not a number. nothing is equal to a NaN